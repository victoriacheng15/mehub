---
import { formatDate } from "@/utils";

interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time class="text-primary-100 text-sm" datetime={date.toISOString()}>
	{formatDate(date)}
</time>
