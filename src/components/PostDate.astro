---
import { formatDate } from "@/utils";

interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time class="text-sm text-primary-100" datetime={date.toISOString()}>
	{formatDate(date)}
</time>
