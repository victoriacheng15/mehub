---
import { Image } from "astro:assets";
import { siteConfig, formatSocialName } from "@/config";

const SIZE = 35;
---

<div class="flex flex-wrap gap-6">
	{
		siteConfig.socials.map(({ name, href }) => (
			<a
				href={href}
				aria-label={name}
				class="bg-secondary-200 hover:bg-secondary-400 rounded-md duration-300 ease-in-out"
				target="_blank"
				rel="noopener noreferrer"
			>
				<Image
					width={SIZE}
					height={SIZE}
					src={`/socials/${formatSocialName(name)}.svg`}
					class="rounded-md p-1"
					alt={name}
				/>
			</a>
		))
	}
</div>
