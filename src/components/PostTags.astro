---
interface Props {
	tags: string[];
}

const { tags } = Astro.props;
---

<div class="mt-4 flex flex-wrap gap-2">
	{
		tags.map((tag) => (
			<a href={`/tags/${tag}`} class="tag">
				#{tag}
			</a>
		))
	}
</div>
