---
interface Props {
	tags: string[];
}

const { tags } = Astro.props;
---

<div class="mt-4 flex flex-wrap gap-2">
	{
		tags.map((tag) => (
			<a
				href={`/tags/${tag}`}
				class="bg-secondary-900 hover:bg-secondary-800 rounded-full px-3 py-1 text-sm text-white duration-300 ease-in-out"
			>
				#{tag}
			</a>
		))
	}
</div>
