name: Markdown Linter

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  call-markdownlint:
    uses: victoriacheng15hub/platform-actions/markdown-linter@main
    with:
      md_paths: |
        src/content/blog/**/*.md